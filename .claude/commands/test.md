Run the City Unblocked test suite by executing `node run-tests.js` in the project root directory. Report the number of tests passed and failed across all three suites (test-mapgen, test-effects, test-balance). If any tests fail, show the failure messages and stop — do not proceed with any further work until the failures are fixed.
